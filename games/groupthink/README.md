# Groupthink

Groupthink is the first playable Room Riot game. Players answer the same prompt privately, then matching answers are grouped and scored by the server.

Content lives in `content/*.json`; rules and scoring live in `src/index.ts`.
